import 'package:flutter/material.dart';

class ImageCategoryMobileView extends StatelessWidget {
  const ImageCategoryMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("images"),
    );
  }
}
